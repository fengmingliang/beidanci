我要基于github pages做一个在线单词背诵的系统，纯网页方案实现，直接生成可用的html文件，要求如下：
布局要清新雅致，显得高级，配色简介鲜艳适合小学生
PC网页版要求：左侧为设置栏目，浏览器左侧，减少占用窗口；右侧为答题区，手机或pad上浏览时，从上往下依次展示设置和题目内容
可以选择背诵模式、复习错词和考题模式的下拉选项窗，一个开始按钮，点击后按照选择的模式进入练习。
设置下方增加一个单词库选择按钮，按钮下拉菜单里罗列出所有与html同目录下的txt文件名作为词库，默认“初中”，导入"初中.txt”的词库
1、背诵模式：可设置显示停留时间，单位S，默认时间3S； 原始词库来自html同目录本地文件，文件名为“初中.txt”，单词库是txt格式文档，每行一个单词及释义，存储格式是“单词:音标:释义（词性）”，记录的错词本也按照这个格式记录整行。
先显示单词和音标，不显示汉语意思，间隔设置的停留时间后展示单词；单词下方显示答对了和答错了的按钮供用户选择，选择完成则继续下一个单词； 如果选择答错了，则把这个单词在词库的位置记录在本地错词本中，方便后续在复习错词模式里选取这些错词复习。
第一次启动时，在单词词库范围内生成一个随机序列，出题按照这个随机序列选择单词本的行号用于出题，并在本地记录当前背诵的单词位置，下次启动则从上次记录的位置开始，如果没有这个记录则从头开始；

2、复习错词模式：方式同背诵模式，只是单词词库来自错词本，如果找不到错词本或这为空则提示没有错词。
如果在答这个词语时选择了答对了，则从错词本中删除

3、考题模式：从选择词库txt文件里读取单词库，单词库是txt格式文档，每行一个单词及释义，存储格式是“单词:音标:释义（词性）”
可以设置题目数量，一次默认20个，全部为选择题
题目就是单词和音标，答案其中一个为正确释义，其他为随机来自单词本的其他单词释义
用户直接选择答案，如果正确答案窗口显示绿色，如果错误显示红色
最后出现提交按钮，显示首次答对的题目数量和得分。

by：智谱清言